# Minara Cross-Sectional Factors — Korean

The Korean source folder was empty apart from an empty `assets/` directory at backup time. No Korean HTML, audio, video, or other project media was available to copy.

When Korean source files become available, serve this directory with Python's standard library:

```sh
python3 -m http.server 8000 --directory .
```

