# S10–S13 · Autopilot proven ones · v5

Status: approved · 2026-09-08. Preview `_test-v5.html`. 1920×1080, 30fps, 15 seconds including the opening title. Approved MP4: `../../output2/s10-s13-autopilot-proven-ones-v5.mp4`.

## the project owner prompt

s10-s13 content
 find your fit Or Autopilot proven ones
one15
 
find findone、、 ，run

 portfolio ，strategy cards、oneone

## Implementation

- 0–2s: exact `Or Autopilot proven ones`, using the installed official SnapCN TextSwap source. Regular Geist, no added punctuation.
- 2–5s: Marketplace, six reference-style cards, scroll to Adaptive Trend Follower and click its center. Search and Sharpe/drawdown sorting are functional in manual mode.
- 5–7.7s: DOM/SVG strategy detail follows `references/detail-reference.png`. Sequential emphasis: Sharpe 4.38, backtest drawdown 6.74%, 90-day live return +24.80%. Backtest/live equity lines and Publish marker are redrawn vectors.
- 7.7s: Run; button and explanatory wallet state update.
- 9.05s: click Portfolio; three compact cards grow in with clipped sheens. Two are running and one is paused. Four overlapping subscriber portraits on each, no favorite/action bottom row.
- 11.85s: pause the first card; 12.55s: start the second. Dollar values flip upward above cards; the second uses +$25, +$80, +$200.

## Preserved constraints and explicit overrides

All data is fixed mock data. Gray-white flow background; positive, upward-trending curves; GSAP/Minara easing. All text is regular 400. The new request overrides the original separate scene duration and title; the prior 600-weight style default is overridden by the project owner's no-bold requirement. New version is isolated; original S10 and S12/S13 v3 and MP4s are retained. All future exports go to `codex/output2/`.

References are measurement inputs only and never rendered as screenshot textures. Title source is byte-identical to the official component already installed for S10; only its caller text, weight and timing changed.

## Verification

`node codex/scenes/s10-s13-v5/verify.mjs` passed. Main agent inspected title entrance/hold/exit, browsing, detail emphasis, Portfolio, and actual manual handoff screenshots. Validated all five cursor click targets, 15s duration, local assets, no bold, overlapping avatars, working search/sort, run/pause/start behavior, and seek/replay restoration. Evidence: `verification/`.

2026-09-08 v5 refinement: removed the “Running 2 · Paused 1” summary entirely. Marketplace rows and Portfolio cards now center on canvas y=540; the detail panel is centered at (960,540) with a 0.9 display scale. Navigation and above-card profits have clearance; cursor coordinates were updated. Browser verification passed for all centered layouts and the full manual click flow. Main agent inspected marketplace/detail/portfolio screenshots. V4 remains unchanged.

original wording：  

2026-09-08 render delivery: `../../output2/s10-s13-autopilot-proven-ones-v5.mp4` · 1920×1080 · 30fps · 450 frames · 15.000s · silent H.264 CRF13, yuv420p BT.709. Renderer `render.mjs` reads `_test-v5.html`; readiness includes the official title player. Metadata assertions passed with no page errors. Main agent reviewed the renderer diff and the actual encoded contact sheet / final frame in `render-verification/`. Older versions remain intact.
