# S18 + S19 · Your models. One workspace.

**2026-09-08 · Direct-logo version approved and exported.** `codex/output2/s18-s19-minara-direct-logo-gfi-ending.mp4` — 1920×1080, 30fps, 354 frames, 11.800s, silent H.264. Complete logo already appears in wheel and enlarges directly to center, continuously opaque; no intermediate Geist identity or Harness. General Financial Intelligence holds two seconds before minara.ai. Encoded contact sheet inspected and export metadata assertions passed with no runtime errors or remote requests. Evidence: `codex/output2/s18-s19-direct-logo-render/verification/`. Previous exports retained. Supersedes pending-preview status below.


**2026-09-08 · Current preview: historical direct-logo transition restored.** User clarified complete logo must already appear inside wheel, then move/enlarge directly to center. Historical reference found and visually confirmed: `codex/output/s18-s19-light-wheel-ending-rounded-cursor.mp4` and its encoded click frame. Restored the same source `assets/minara-logo-light.png` for wheel and hero, with matching size/position at the 6.30s fully opaque handoff. Removed intermediate avatar + Geist and delayed image switch entirely. Hero ends at 720px width; no Harness; 56px GFI, two-second hold and minara.ai ending preserved. Browser checks passed; wheel/click and centered logo screenshots inspected in `codex/output2/s18-s19-direct-logo-preview/`. Preview not yet rendered; next output is `codex/output2/s18-s19-minara-direct-logo-gfi-ending.mp4`. This supersedes the earlier wheel-Geist instruction per latest clarification.


**2026-09-08 · Current alternate export: official ending logo + GFI, no Harness.** Per latest request, wheel retains standalone avatar + regular Geist Minara. After click/enlargement, identity fades out at 7.20–7.32s and the complete, unchanged supplied `minara-logo-light.png` fades in at 7.32–7.50s, centered at 720px width with native proportions. No split/cropped logo and no Harness display. Logo lifts 60px; 56px General Financial Intelligence appears below and holds two seconds before the existing stationary minara.ai ending. Export: `codex/output2/s18-s19-minara-logo-gfi-ending.mp4` — 1920×1080, 30fps, 354 frames, 11.800s. Browser assertions and metadata passed; encoded contact sheet inspected; no runtime errors or remote requests. Evidence: `codex/output2/s18-s19-logo-gfi-render/verification/`. Earlier Geist/Harness exports retained. This restores official logo usage only in the ending, superseding the previous all-Geist variant for this version.


**2026-09-08 · Current approved export: standalone avatar + Geist.** Brand feedback supersedes all earlier image-wordmark instructions: Minara is now regular Geist in both wheel and ending, beside the supplied standalone `assets/providers/minara-square.png`. The composite logo PNG is no longer used in the scene. Matched wheel/hero typography preserves the fully opaque click handoff. Final Minara/Harness type is 112px regular Geist with a tight gap and aligned baseline; 56px subtitle, two-second hold and stationary URL fades remain. Export: `codex/output2/s18-s19-minara-geist-harness-ending.mp4` — 1920×1080, 30fps, 354 frames, 11.800s. Browser checks and export metadata passed; encoded contact sheet inspected; no runtime errors or remote requests. Evidence: `codex/output2/s18-s19-geist-render/verification/`. Previous files retained as historical versions; do not reuse their wordmark styling.


**2026-09-08 · Proportions approved and exported.** `codex/output2/s18-s19-minara-harness-gfi-proportions-ending.mp4` — 1920×1080, 30fps, 354 frames, 11.800s, silent H.264. Includes final avatar reduced 20% and 56px regular subtitle. Encoded contact sheet inspected; render metadata assertions passed, no runtime errors or remote requests. Evidence: `codex/output2/s18-s19-gfi-proportions-render/verification/`. Previous exports preserved. Supersedes pending-preview status below.


**2026-09-08 · Current proportions preview, not yet rendered.** Final avatar is 20% smaller, with the supplied PNG displayed as separate CSS-clipped avatar/wordmark regions; original asset pixels are unchanged. Avatar adjusts during Harness entrance only, preserving the opaque click handoff. Wordmark, Harness size/alignment and tightened gap remain unchanged. Subtitle increases from 48px to 56px regular Geist. Duration and two-second hold unchanged. Targeted browser assertions passed and screenshot inspected: `codex/output2/s18-s19-gfi-proportions-preview/full-lockup.png`. Next approved render target: `codex/output2/s18-s19-minara-harness-gfi-proportions-ending.mp4`. Revert: restore single brand image, remove avatar tween, return subtitle to 48px.


**2026-09-08 · GFI ending approved and exported.** `codex/output2/s18-s19-minara-harness-gfi-ending.mp4` — 1920×1080, 30fps, 354 frames, 11.800s, silent H.264. Raised Minara Harness + General Financial Intelligence holds two seconds, followed by sequential fades to minara.ai. Encoded contact sheet inspected; metadata assertions passed; no runtime errors or remote requests. Evidence: `codex/output2/s18-s19-gfi-render/verification/`. Previous MP4 preserved. This supersedes the pending-approval note below.


**2026-09-08 · Current preview: General Financial Intelligence.** After the Minara Harness lockup settles, both elements lift 60px at 7.90–8.35s, preserving their baseline and tightened gap. Regular 48px Geist subtitle appears beneath at 8.10–8.50s. The full composition holds exactly 8.50–10.50s, fades out together over 160ms, then the existing 72px minara.ai fades in. Duration: 11.80s / 354 frames. Browser verification passed (lift, subtitle, two-second hold, no overlapping ending, stable click/hover); full-lockup screenshot inspected. Evidence: `codex/output2/s18-s19-gfi-preview/`. Preview awaiting approval; renderer prepared for `codex/output2/s18-s19-minara-harness-gfi-ending.mp4`, not rendered. Previous approved MP4 is preserved. Revert by removing tagline element/style/tweens, restoring logo y=0 / Harness y=8.5, ENDING=9.7 and duration=11.0.


**Approved and exported:** aligned Minara Harness lockup with word gap halved. MP4: `../../output2/s18-s19-minara-harness-ending.mp4`, 1920×1080 / 30fps / 11.00s / 330 frames. Encoded contact sheet inspected; metadata assertions passed with no runtime errors or remote requests. Evidence: `render-verification-minara-harness/`. Earlier preview-only notes below are historical.

**CURRENT PREVIEW: Minara Harness lockup.** After logo enlargement completes at 7.00s, at 7.20s the unchanged supplied logo shifts 245px left and reduces to 78%; “Harness” enters on its right in regular Geist via the installed TextSwap and GSAP spring easing. Completed lockup holds 7.70–9.70s, then both fade out in place before the existing 72px minara.ai fade. Total **11.00s / 330 frames**. Earlier ripple, stable hover and opaque click handoff retained. Browser check passed and `verification/minara-harness/minara-harness.png` inspected. Preview only; next render destination is `../../output2/s18-s19-minara-harness-ending.mp4`. Earlier exports below are historical.

**Latest approved render delivered:** `../../output/s18-s19-light-wheel-ending-rounded-cursor.mp4` — 1920×1080 / 30fps / 309 frames / 10.300s. Includes larger rounded cursor, stable hover, continuously opaque logo handoff, ripple and 72px ending URL. Encoded click frame inspected; metadata checks passed without runtime errors. Evidence: `render-verification-light-ending-rounded-cursor/`. Earlier pending-render notes are history; older outputs preserved.

Latest cursor revision: reference-inspired rounded charcoal triangular pointer, 60×64px instead of 30×40px, with a soft white outline. SVG redraw; reference pixels/watermark are not used. Stable hover, continuously visible click handoff and spring ripple remain verified. This update is being included in the pending approved render, targeting `s18-s19-light-wheel-ending-rounded-cursor.mp4`.

Latest correction: hover no longer translates any logo. At 6.30s the wheel logo switches atomically to the identical fully opaque hero image at matching position/size, then enlarges; there is no click fade or disappearance. Ripple and the later intentional logo→URL fades remain. Browser check samples opacity, monotonic width and fixed horizontal center across the handoff, plus actual hover stability. Screenshot: `verification/light-ending/logo-handoff.png`. Preview only.

Latest preview revision: removed shrinking at the Minara click. Root cause was heroOpacity multiplying the wheel logo scale; opacity is now independent. Cursor size also remains fixed. Two thin, restrained spring/echo ripples expand from the click point. Existing logo enlargement, two-second hold and stationary ending fades retained, URL remains 72px. Updated browser check asserts no shrink and correct ripple placement; screenshot `verification/light-ending/click-ripple.png` inspected. Not rendered yet; previous MP4 remains the older version.

Latest revision exported and verified: minara.ai is now 72px, reduced 50% from 144px. Approved MP4: `../../output/s18-s19-light-wheel-ending-small-url.mp4`, 1920×1080 / 30fps / 309 frames / 10.300s. Encoded final frame inspected; metadata checks passed with no runtime errors. Evidence: `render-verification-light-ending-small-url/`. Earlier size/export notes below are history.

the project owner approved the final stationary-fade light version with “render”. **Exported and verified:** `../../output/s18-s19-light-wheel-ending.mp4` — 1920×1080, 30fps, 309 frames, 10.300s, silent H.264. Encoded contact sheet, ending fade frames and final frame inspected; no runtime errors or remote requests. Evidence: `render-verification-light-ending/`. Earlier pending-review notes below are historical.

Latest ending correction: logo fades out in place over 160ms at 9.00s; minara.ai then fades in in place over 160ms. No vertical movement, scaling or overlapping visible text during this switch. Ending TextSwap is held on its settled frame and GSAP controls only opacity, per the latest request; opening TextSwap is unchanged. Browser samples and `verification/light-ending/fade-in-place.png` confirmed. Preview only.

**CURRENT: Light minimal wheel → ending, preview awaiting approval.** Supersedes all earlier color-card/green-sweep exports below. User requested no card backgrounds, pure logo + regular text, pale-white background, then the supplied `Minara Logo Text-Light Mode.png` enlarged at center. This file is copied unchanged to `assets/minara-logo-light.png` and also replaces typed Minara in the wheel. At 6.18s the cursor clicks; logo settles at center by 7.00s, holds through 9.00s, then rapidly transitions to `minara.ai` with the actual installed TextSwap component. Total now **10.30s / 309 frames / 30fps**, directly incorporating the ending. Title also retains actual TextSwap, now dark on light. Background reuses `codex/assets/bg/bg-light-flow.mp4`. No green fill, badges, chevrons or small heading. Manual Minara selection plays the ending. Screenshots and updated `verify.mjs` passed: `verification/light-ending/`. `render.mjs` is prepared for a new `s18-s19-light-wheel-ending.mp4`, but has not been run. Earlier exports remain intact. Assembly should use this as the ending, avoiding a second outro.

**Final approved Apple wheel MP4 exported and verified:** `../../output/s18-s19-your-models-one-workspace-apple-wheel.mp4` — 1920×1080, 30fps, 231 frames, 7.700s, silent H.264. Includes the removed heading and all approved visual revisions. Encoded contact sheet/final frame inspected; no runtime errors or remote requests. Evidence: `render-verification-apple-wheel/`. `render.mjs` now targets this version. Previous MP4 is retained; earlier pending-review statements below are historical.

Current refinement: Apple-style wheel picker per the supplied date-picker reference. Center card stays sharp and full-strength; neighboring cards tilt gently (approximately 21° per row), fade progressively and soften toward the edges. Large brand-color cards, regular typography and stop → click → green timing retained. Preview and manual controls verified; screenshots in `verification/apple-wheel/`. Awaiting approval, no new render.

Status: **Apple wheel approved for MP4 export**. the project owner requested “render to mp4” after the Apple wheel and heading removal. Earlier review notes below are history. Latest visual requests: “  minara   ”, “ logo”, “Typography”.

Latest refinement: “ 3D ”. Cards now project onto a cylinder: the center card is approximately 1140px wide, while adjacent cards tilt and recede. GSAP drives one shared scroll position for playback and wheel/keyboard interaction. The click still precedes the final 12% enlargement and green sweep. Verified screenshots/checks: `verification/fisheye/`. Preview only.

Brand-card clarification: the project owner supplied an orange Anthropic card with a white Claude star and regular white type. Applied full-card color (Anthropic #d97757, OpenAI green, OpenRouter lime, Kimi blue, xAI/Ollama charcoal) and white transparent logos on dark/color cards. Anthropic star comes from the local Lobe `claude.svg`; Minara keeps the supplied square avatar and turns green only after click. Screenshot inspected: `verification/fisheye/anthropic-brand-card.png`.

Current preview: logo-color rounded provider cards scroll upward from OpenAI through OpenRouter, Anthropic, Kimi, xAI and Ollama to Minara. Reel stops at 5.55s, cursor clicks at 6.18s, green sweep starts at 6.25s and enlargement follows at 6.30s. Heading, labels and official TextSwap headline use regular weight. Square avatar and rounded sweep remain. Wheel and arrow keys scroll the manual reel; click/Enter/Space selects. Verification and screenshots: `verification/reel/`. This revision has not been rendered; the MP4 below is the previous version.

Output: `../../output/s18-s19-your-models-one-workspace.mp4` — 1920×1080, 30fps, 231 frames, 7.700s, silent H.264 CRF13 / yuv420p / BT.709. Reproduce with `node codex/scenes/s18-s19/render.mjs` from the project root. Source UI is unchanged from the approved preview. Metadata assertions and encoded-video title, hover, original-size click, expanding green fill and final square-avatar selection frames passed inspection. Evidence: `render-verification/render.json`, `contact-sheet.jpg`, `finalframe.png`.

Preview: `_test.html` (local file, no server). Scene source: `scene.js`, `scene.css`; official title source: `title/`. Verification: `node codex/scenes/s18-s19/verify.mjs` from the project root.

## Timing and interpretation

Original range preserved: 61.333–69.033s, **7.700s / 231 frames**, 1920×1080, 30fps. S18 occupies the first 1.834s. Headline is exactly `Your models. One workspace.`

The latest upward reel replaces the earlier unfolding list and downward cursor tour. OpenAI starts “In use”; the badge transfers to Minara on the click. The first exported version described below is retained as history.

Both automatic and manual interactions use the same hover and sweep animation functions. “Try the UI” exposes real local selection, hover, keyboard navigation and exclusive checked state. There are no model requests or account changes.

## the project owner's prompt (verbatim)

```text
storyboard Project rules

s18-s19 work together

Your models. One workspace.

modeltab
 eachhover
minara
reference slide across

---

Background

UIUI GSAP minaraMotion

keep the product believable and usable in the video、interactions should also work as real interactions

show me a test page for approval first render

claude 
./claude

codex 
./codex

use this template for every large-title entrance pnpm dlx shadcn\@latest add @snapcn/text-swap Motion

Motion do not ignore the template I provided
```

## References and provenance

- Supplied images preserved as `references/model-providers.png` and `references/selection-sweep.png`. They are measurement references only, never textures or crops in the scene.
- UI rows, labels, badges, chevrons and feedback are DOM/SVG redraws. Geometry follows the supplied 391×67px rows at approximately 1.5× size.
- Minara avatar: the user-supplied `Documents/minara for human/Minara media kit 2026-8-26/Minara avatar square.png`, copied unchanged into `assets/providers/minara-square.png` (1600×1600). Replaces the older circular portrait after explicit feedback.
- OpenRouter: existing product `apps/web-ui/src/components/shared/openrouter.svg` from the local `minara-agent-office-prompts-current` worktree. Exact Volt glyph matching the reference; the older bifurcating-arrow icon is not used. No product source was modified.
- OpenAI, Anthropic, Moonshot (the reference's Kimi orb), xAI and Ollama: local Lobe Icons static SVG assets. Original vector paths retained.
- GSAP, CustomEase, existing gray-black flowing media and local Geist fonts reused from the existing project. Entrance `.16,1,.3,1`; Minara camera `.95,.03,0,.98`.
- **Actual official `@snapcn/text-swap`**, installed successfully with `pnpm dlx shadcn@latest add @snapcn/text-swap --yes`. Component motion source is unchanged. Font resolution alone uses local Geist. See `title/PROVENANCE.md`; rebuilt IIFE in `title/assets/title.js`.
- Explicit prompt takes precedence over the generic color restriction for the selected row's green fill and the reference OpenRouter glyph. The surrounding composition remains gray/black.

## Verification · 2026-09-07

`verify.mjs` passes: 7.7s duration; eight headless screenshots at 1600×1000; full composition and all seven rows fit; all seven hover states and cursor positions; final click position; monotonic left-to-right sweep; selected badge transfer; backward/forward seek; all seven manual selections; keyboard wrap; no runtime errors or network requests. Media seek checked at 0.7 and 6.7s. Key screenshots personally inspected after fixing preview scaling and premature click-ring visibility.

No server or rendering process remains running. Awaiting test-page review; do not render until the project owner explicitly approves.

## Feedback · continuous cursor glide

the project owner: “ ”. Replaced the seven separate move-and-hold tweens with one continuous sine-eased descent. Hover entry and exit now follow the actual row crossings; the cursor returns to Minara immediately after the descent. Original timing, title, UI and selection sweep retained. The browser check samples every frame of the descent and rejects any pause. Preview only; no render.

the project owner: “minara minara tab” and “”. The cursor now clicks the original-size row before enlargement; other rows remain visible until after selection. Replaced the portrait with her supplied square avatar, unchanged. Browser checks assert original size at click, enlargement afterward, and the supplied 1600×1600 image; click and enlarged-state screenshots inspected. Still awaiting preview approval; no render.

the project owner: “ ”. Removed the separate vertical selection-edge strip and its glow/animation from automatic and manual selection. The sweep is now the rounded green fill alone, without a straight accent at its advancing edge.
