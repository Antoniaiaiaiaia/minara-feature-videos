# Beyond finance / Computer use

Static 24.1-second DOM/SVG film scene. Open `_test.html` without a server. Review controls are outside the fixed 1920×1080 stage.

- `scene.js`: GSAP timeline, document stacking, frame-driven cursors/typing, native local form handling and downloads.
- `scene.css`: additions to the prior scene's faithful client and PowerPoint redraws.
- `title.tsx`: actual installed Snapcn TextSwap opening and Computer use heading; exports installed AnswerStream helpers.
- `node build-title.mjs`: rebuild the title bundle using existing dependencies.
- `node render.mjs`: reproduce the approved 723-frame MP4 in `../../output2/s16-s17-computer-use.mp4`; protects existing output. All future exports use `output2`; previous exports remain in `output`.
- `node verify.mjs`: focused browser verification and screenshots, 1600×1000 review viewport.

Reuse assets, installed registry sources and PPTX from the sibling earnings scene. Keep that earlier approved version and output intact. All browser data is fictional and all submissions are local. Existing NVIDIA figures are sourced in the sibling scene's research folder.

Every animated state must survive forward/backward seeking. Give GSAP fromTo tweens explicit final visibility when the window can be revisited. Cursor motion uses a common GSAP-eased path; larger cursors keep parallel actions visible after windows shrink. Keep headings on the real TextSwap component and typing on the installed AnswerStream timing helper. The current revision is approved and exported at 723 frames / 30 fps. New revisions need preview approval before rendering.
